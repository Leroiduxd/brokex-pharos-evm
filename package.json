{
  "name": "brokex-pharos-deploy",
  "version": "1.0.0",
  "description": "Hardhat deployment for Brokex on Pharos Testnet",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network pharos"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "ethers": "^6.10.0",
    "hardhat": "^2.22.0"
  }
}
